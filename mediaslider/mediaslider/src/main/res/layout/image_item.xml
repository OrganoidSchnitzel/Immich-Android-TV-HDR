<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <nl.giejay.mediaslider.view.TouchImageView
        android:id="@+id/mBigImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerInside"
        android:src="@drawable/images"
        android:layout_centerInParent="true"
        />
    <!-- Only made visible for an Ultra HDR photo, and the image view above is hidden at the same
         time, so this layer is what reaches the screen. Gone the rest of the time, which is when
         a SurfaceView costs nothing. -->
    <nl.giejay.mediaslider.hdr.HdrImageSurfaceView
        android:id="@+id/hdr_image_surface"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />
    <ProgressBar
        android:id="@+id/mProgressBar"
        android:visibility="visible"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:indeterminate="true"
        android:layout_centerInParent="true"
        android:theme="@style/ProgressBar"/>

</RelativeLayout>
